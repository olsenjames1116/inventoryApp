extends layout 

block content 
    h1 Category: #{category.name}

    h4 Items
        dl 
            each item in itemList 
                dt 
                    a(href=item.url) #{item.name}
                dd #{item.description}
            
            else
                p This category has no items.
